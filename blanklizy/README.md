# بلانكليزي (Blanklizy)

اختصار آيفون (Apple Shortcuts) لتحميل الفيديوهات من مواقع التواصل الاجتماعي، بدون أي سيرفر خاص — الاختصار يتواصل مباشرة مع خدمات عامة مجانية لجلب رابط الفيديو ثم يحفظه في تطبيق الصور.

## التثبيت على الآيفون

1. حمّل ملف [`Blanklizy.shortcut`](./Blanklizy.shortcut) على آيفونك (عبر AirDrop، أو رابط تحميل مباشر، أو تطبيق الملفات).
2. اضغط على الملف لفتحه في تطبيق **الاختصارات (Shortcuts)**.
3. لأن الملف غير موقّع رقمياً (unsigned) من Apple/RoutineHub، ستحتاج أول مرة فقط لتفعيل:
   **الإعدادات ← الاختصارات ← السماح بالاختصارات غير الموثوقة (Allow Untrusted Shortcuts)**
   (هذا الخيار يظهر فقط بعد محاولة استيراد اختصار غير موقّع مرة واحدة).
4. اضغط "إضافة اختصار" (Add Shortcut).

## طريقة الاستخدام

- **من مشاركة (Share Sheet):** افتح الفيديو في تطبيق تيك توك أو تويتر/X، اضغط مشاركة، واختر "Blanklizy".
- **يدوياً:** افتح اختصار Blanklizy من تطبيق الاختصارات أو الشاشة الرئيسية، والصق رابط الفيديو عند الطلب.

الفيديو يُحفظ تلقائياً في تطبيق الصور داخل ألبوم باسم "Blanklizy".

## المنصات المدعومة حالياً

| المنصة | الحالة |
|---|---|
| TikTok | ✅ يعمل (بدون علامة مائية، عبر tikwm.com) |
| Twitter / X | ✅ يعمل (عبر api.vxtwitter.com) |
| Instagram | ⏳ غير مدعوم بعد |
| Facebook | ⏳ غير مدعوم بعد |
| YouTube | ❌ غير مدعوم — يوتيوب يحجب هذه الطريقة (بدون سيرفر خاص) بشكل صارم، ولا توجد حالياً خدمة مجانية موثوقة تسمح بذلك من اختصار وحده. للتحميل من يوتيوب استخدم أداة مثل `yt-dlp` على جهاز كمبيوتر |

**لماذا Instagram/Facebook وYouTube غير مدعومة؟** هذه المنصات تتطلب إما تسجيل دخول/جلسة مصادقة أو تقنيات معقدة (مثل فك تشفير التوقيعات) لا يمكن تنفيذها داخل اختصار بدون سيرفر خلفي. الخدمات المجانية العامة التي تدعم ذلك تتغير أو تتوقف باستمرار. يمكن إضافتها لاحقاً كنسخة "أفضل جهد" أو ببناء سيرفر بسيط خاص بالمشروع إذا رغبت.

## الخصوصية

الاختصار لا يملك أي سيرفر خاص به. يرسل فقط رابط الفيديو الذي تدخله إلى:
- **tikwm.com** لروابط TikTok
- **api.vxtwitter.com** لروابط Twitter/X

لا يُجمع أو يُخزَّن أي بيانات من قِبل هذا المشروع.

## البناء من المصدر

الاختصار مكتوب بلغة [Cherri](https://cherrilang.org) (ملف [`Blanklizy.cherri`](./Blanklizy.cherri)) وهي لغة تُترجم مباشرة إلى ملف Shortcut صالح، مما يسهّل تعديل المنطق ومراجعته كنص عادي بدل التعامل مع تطبيق الاختصارات مباشرة.

لإعادة البناء:

```bash
# تثبيت مترجم Cherri (macOS: Homebrew / Nix، أو أي نظام عبر Go)
git clone https://github.com/electrikmilk/cherri
cd cherri && go build -o cherri .

# البناء بدون توقيع (يتطلب "Allow Untrusted Shortcuts" عند الاستيراد)
./cherri Blanklizy.cherri --skip-sign
```

## خارطة الطريق

- إضافة دعم أفضل-جهد لإنستغرام وفيسبوك.
- استكشاف توقيع الاختصار (`--hubsign` أو signing server) لتفادي خطوة "Allow Untrusted Shortcuts".
